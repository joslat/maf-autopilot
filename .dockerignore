# Keep the build context lean — only what the Dockerfile actually COPYs.
**/bin
**/obj
.git
.github/scripts
.vscode
docs
!docs/steering
!docs/steering/**
# docs/migration/ is embedded as a csproj resource (SK→MAF guide, added 1.5.0);
# it must be in the build context or in-Docker `dotnet publish` fails (CS1566).
!docs/migration
!docs/migration/**
guides
!guides
src/maf-autopilot.Tests
src/maf-autopilot.Analyzers
src/maf-autopilot.Analyzers.Tests
*.sln
LICENSE
CONTRIBUTING.md
TROUBLESHOOTING.md
types.txt
