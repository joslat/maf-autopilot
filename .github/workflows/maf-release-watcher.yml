name: MAF Release Watcher

on:
  schedule:
    - cron: '0 6 * * 4'   # Thursday 06:00 UTC (~08:00 Europe/Zurich) — aligned with MAF's .NET Thursday-morning ship cadence. Manual workflow_dispatch covers off-cadence releases.
  workflow_dispatch:        # Manual trigger anytime
    inputs:
      maf_version:
        description: 'Optional next pending stable MAF version. Must equal the oldest stable newer than .maf-version; leave blank to select it automatically.'
        required: false
        default: ''
      push_target:
        description: 'Override branch for watcher commits (dry-run). Leave blank for the default release-watcher/maf-<version> PR branch (the scaffold is opened as a PR, never pushed to main).'
        required: false
        default: ''

# Phase 3.3 — a run can never overlap itself. An overlap could create competing
# release branches from the same tracked version. cancel-in-progress is false so
# an in-flight real update is allowed to finish.
concurrency:
  group: maf-self-update-watcher
  cancel-in-progress: false

# SEC-26: default-deny at the workflow level. Jobs that only read the repo inherit
# this least-privilege grant; the privileged analyze-and-update / notify jobs
# override it with their own broader blocks below.
permissions:
  contents: read

jobs:
  # Phase 3.1 — validate workflow_dispatch input BEFORE any privileged step
  # runs. The watcher uses COPILOT_ASSIGN_PAT for its final push; a
  # malformed `maf_version` would otherwise template into shell-interpreted
  # contexts downstream.
  validate-inputs:
    runs-on: ubuntu-latest
    permissions: {}
    steps:
      - name: Validate maf_version input shape
        env:
          INPUT: ${{ inputs.maf_version }}
        run: |
          if [ -z "$INPUT" ]; then
            echo "No version input — will use the oldest pending stable from NuGet"
            exit 0
          fi
          if ! echo "$INPUT" | grep -qE '^[0-9]+\.[0-9]+\.[0-9]+$'; then
            echo "::error::Invalid maf_version input shape: $INPUT (must be a stable X.Y.Z version)"
            exit 1
          fi

      - name: Validate push_target input shape
        # Task 5.2 — push_target lets a maintainer dry-run analyze-and-update
        # against a throwaway branch. Validate it's a plausible branch name
        # (defense-in-depth; it's also env-redirected at the push site).
        env:
          PT: ${{ inputs.push_target }}
        run: |
          if [ -z "$PT" ]; then exit 0; fi
          if ! echo "$PT" | grep -qE '^[A-Za-z0-9._/-]+$'; then
            echo "::error::Invalid push_target: $PT (branch-name characters only)"
            exit 1
          fi

  check-for-new-maf-release:
    needs: validate-inputs
    runs-on: ubuntu-latest
    # SEC-26: explicit least-privilege. contents:read to read .maf-version;
    # pull-requests:read for the WM-12 already-open-PR short-circuit below.
    permissions:
      contents: read
      pull-requests: read
    outputs:
      new_version: ${{ steps.version-check.outputs.new_version }}
      old_version: ${{ steps.version-check.outputs.old_version }}
      has_update: ${{ steps.version-check.outputs.has_update }}
      is_major: ${{ steps.version-check.outputs.is_major }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0

      - name: Check NuGet for new MAF version
        id: version-check
        # Phase 3.2 — read inputs via env: redirect; reference $INPUT_VERSION as
        # a shell variable. Eliminates template-injection surface in the script.
        env:
          INPUT_VERSION: ${{ inputs.maf_version }}
          PUSH_TARGET: ${{ inputs.push_target }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}   # WM-12: gh pr list short-circuit
        run: |
          CURRENT=$(cat .maf-version | tr -d '[:space:]')
          echo "Current tracked version: $CURRENT"

          # Always resolve the OLDEST pending stable release from NuGet, even
          # for a manual dispatch. An explicit version is an assertion, not a
          # bypass: it must equal that next release, so 1.13 -> 1.17 cannot skip
          # the 1.14/1.15/1.16 migration guides and registry cycles.
          for attempt in 1 2 3; do
            RAW=$(curl -sS --max-time 30 "https://api.nuget.org/v3-flatcontainer/microsoft.agents.ai/index.json" || true)
            if [ -n "$RAW" ]; then break; fi
            echo "NuGet query attempt $attempt failed, retrying after $((attempt * 5))s..."
            sleep $((attempt * 5))
          done
          if [ -z "$RAW" ]; then
            echo "::error::NuGet API unreachable after 3 attempts — release state is unknown; failing closed."
            exit 1
          fi
          NEWEST=$(echo "$RAW" | python3 .github/scripts/get_latest_stable.py)
          NEXT=$(echo "$RAW" | python3 .github/scripts/get_latest_stable.py --after "$CURRENT")
          if [ -z "$NEWEST" ]; then
            echo "ℹ️ No stable version found on NuGet — only prereleases. Skipping."
            echo "has_update=false" >> $GITHUB_OUTPUT
            exit 0
          fi
          echo "Latest STABLE version on NuGet: $NEWEST"
          if [ -z "$NEXT" ]; then
            echo "old_version=$CURRENT" >> $GITHUB_OUTPUT
            echo "new_version=$CURRENT" >> $GITHUB_OUTPUT
            echo "has_update=false" >> $GITHUB_OUTPUT
            echo "is_major=false" >> $GITHUB_OUTPUT
            echo "ℹ️ No pending stable release. $CURRENT is current."
            exit 0
          fi
          if [ -n "$INPUT_VERSION" ] && [ "$INPUT_VERSION" != "$NEXT" ]; then
            echo "::error::Requested $INPUT_VERSION, but the next pending stable release after $CURRENT is $NEXT. Process releases sequentially."
            exit 1
          fi
          LATEST="$NEXT"
          if [ -n "$INPUT_VERSION" ]; then
            echo "Validated manually specified next stable version: $LATEST"
          elif [ "$LATEST" != "$NEWEST" ]; then
            echo "ℹ️ Release backlog detected — processing oldest pending stable $LATEST first (newest is $NEWEST)."
          fi

          # WM-12: serialize the backlog. ANY open watcher PR means main still has
          # the old .maf-version, so generating another release from it would create
          # parallel cumulative deltas and skip the per-release chain. A non-empty
          # push_target remains the explicit maintainer dry-run escape hatch.
          if [ -z "$PUSH_TARGET" ]; then
            OPEN_WATCHER=$(gh pr list --state open --json number,headRefName \
              --jq '[.[] | select(.headRefName | startswith("release-watcher/maf-"))][0] | if . then "#\(.number) (\(.headRefName))" else "" end' 2>/dev/null)
          else
            OPEN_WATCHER=""
          fi
          if [ -n "$OPEN_WATCHER" ]; then
            echo "ℹ️ Watcher scaffold PR $OPEN_WATCHER is already open — clean no-op until it is merged or closed."
            echo "old_version=$CURRENT" >> $GITHUB_OUTPUT
            echo "new_version=$LATEST" >> $GITHUB_OUTPUT
            echo "has_update=false" >> $GITHUB_OUTPUT
            echo "is_major=false" >> $GITHUB_OUTPUT
            exit 0
          fi

          echo "old_version=$CURRENT" >> $GITHUB_OUTPUT
          echo "new_version=$LATEST" >> $GITHUB_OUTPUT

          if [ "$LATEST" != "$CURRENT" ]; then
            echo "has_update=true" >> $GITHUB_OUTPUT
            echo "✅ New version detected: $CURRENT → $LATEST"

            # Detect major-version bump (semver MAJOR component changed).
            # `1.3.0 → 2.0.0` triggers escalated treatment; `1.3.0 → 1.4.0` does not.
            OLD_MAJOR=$(echo "$CURRENT" | cut -d. -f1)
            NEW_MAJOR=$(echo "$LATEST" | cut -d. -f1)
            if [ "$OLD_MAJOR" != "$NEW_MAJOR" ]; then
              echo "is_major=true" >> $GITHUB_OUTPUT
              echo "🚨 MAJOR version bump: ${OLD_MAJOR}.x → ${NEW_MAJOR}.x — PR will be escalated."
            else
              echo "is_major=false" >> $GITHUB_OUTPUT
            fi
          else
            echo "has_update=false" >> $GITHUB_OUTPUT
            echo "is_major=false" >> $GITHUB_OUTPUT
            echo "ℹ️ No update. $CURRENT is current."
          fi

  analyze-and-update:
    needs: check-for-new-maf-release
    if: needs.check-for-new-maf-release.outputs.has_update == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: write           # commit the watcher's auto-generated registry/guide updates
      pull-requests: write      # `gh pr create`/`gh pr edit` for the scaffold PR (2026-06-28 "C" refactor)
      issues: write             # Phase 3.2 — escalation issue for major bumps
      # Round-3 review fixup — actions: write REMOVED. It was granted for
      # `gh workflow run maf-ai-fill-todos.yml` (discovered 2026-05-17), but
      # that cross-workflow dispatch was removed by the 2026-06-28 "C"
      # refactor ("we no longer auto-dispatch a SEPARATE Copilot fill PR off
      # main" — see "Summarize next steps" below). Grepped the whole file:
      # no remaining use of the Actions API. Least-privilege — don't carry a
      # scope this job (already contents/pull-requests/issues write plus a
      # maintainer PAT) doesn't use.
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0
        with:
          # Checkout is read-only and must not depend on a long-lived maintainer
          # PAT. An expired COPILOT_ASSIGN_PAT previously prevented even this
          # public-repository fetch, before the workflow could explain the fault.
          token: ${{ secrets.GITHUB_TOKEN }}
          # F-06 (2026-07-19 security assessment) — persist-credentials defaults
          # to true, which embeds this PAT into .git/config for the ENTIRE job:
          # every later step (dotnet-inspect diff, python helpers, dotnet build)
          # could read or reuse it, not just the one step that actually needs to
          # push. Disable it here and authenticate explicitly, once, immediately
          # before the single `git push` call in "Commit to a release-watcher
          # branch and open a PR".
          persist-credentials: false

      - name: Validate maintainer PAT before analysis
        env:
          GH_TOKEN: ${{ secrets.COPILOT_ASSIGN_PAT }}
          GH_REPO: ${{ github.repository }}
        run: |
          if [ -z "$GH_TOKEN" ]; then
            echo "::error::COPILOT_ASSIGN_PAT is missing. Add a repository-scoped fine-grained PAT before running the watcher."
            exit 1
          fi
          if ! gh api user --silent || ! gh api "repos/$GH_REPO" --silent; then
            echo "::error::COPILOT_ASSIGN_PAT is expired, revoked, or cannot read $GH_REPO. Rotate the repository secret before rerunning."
            exit 1
          fi
          echo "Maintainer PAT is valid and can access $GH_REPO."

      - name: Setup .NET
        # Round-4 review fixup — continue-on-error, matching this same file's
        # established pattern on Restore/Build below (a transient
        # network/registry hiccup here previously hard-aborted the whole job
        # — including the version-bump PR it exists to open — instead of
        # degrading gracefully the way the planned-diff runner (continue-on-error
        # below) is explicitly designed to record a missing dotnet-inspect).
        continue-on-error: true
        uses: actions/setup-dotnet@26b0ec14cb23fa6904739307f278c14f94c95bf1  # v5.4.0
        with:
          # Net8 LTS — install-only; we just run the published tool here.
          dotnet-version: '8.0.x'

      - name: Install dotnet-inspect
        continue-on-error: true
        run: |
          # Install dotnet-inspect directly as a global tool.
          #
          # Previously we used `dnx dotnet-inspect@0.7.8` as a one-off launcher,
          # but the `dnx` NuGet package was unlisted in 2026 — the cron failure
          # at 2026-05-11 was "dnx is not found in NuGet feeds". Direct install
          # of dotnet-inspect is the working path now and removes the dnx hop
          # entirely.
          #
          # 0.7.8's Linux Native-AOT binary was built with the .NET 11 preview 3
          # Unix Console regression (dotnet/runtime#126843): direct `> file`
          # redirection overwrote offset zero on every buffered write and left a
          # ~256-byte fragment despite exit 0. dotnet-inspect 0.9.1 is the first
          # upstream release whose CI removed its `| cat > file` workaround after
          # validating the runtime fix (richlander/dotnet-inspect#319/#334).
          INSPECT_VERSION=0.9.1
          dotnet tool install --global dotnet-inspect --version "$INSPECT_VERSION" \
            || dotnet tool update --global dotnet-inspect --version "$INSPECT_VERSION"
          ACTUAL_VERSION=$(dotnet-inspect --version)
          if [ "${ACTUAL_VERSION%%+*}" != "$INSPECT_VERSION" ]; then
            echo "::error::Expected dotnet-inspect $INSPECT_VERSION, got $ACTUAL_VERSION"
            exit 1
          fi
          echo "Installed dotnet-inspect $ACTUAL_VERSION"

      - name: Build targeted MAF package plan
        # The MAF release train does not imply that every package publishes the
        # same literal version. Resolve the checked-in release-critical surface
        # set independently and retain exact package versions in a deterministic
        # manifest-order plan. Missing/ambiguous evidence remains unverifiable.
        env:
          OLD: ${{ needs.check-for-new-maf-release.outputs.old_version }}
          NEW: ${{ needs.check-for-new-maf-release.outputs.new_version }}
        run: |
          python3 .github/scripts/build_maf_package_plan.py \
            --manifest .github/maf-package-surfaces.json \
            --old-version "$OLD" \
            --new-version "$NEW" \
            --output maf-package-plan.json
          cat maf-package-plan.json

      - name: Run and validate planned package diffs
        id: package-diffs
        continue-on-error: true
        run: |
          # Sequential, bounded, manifest-ordered execution. Every surface gets
          # its own stdout/stderr artifacts and result row even when an earlier
          # surface fails. Raw malformed output is retained for audit but never
          # trusted downstream; validation failure classifies breaking.
          python3 .github/scripts/run_maf_package_diffs.py \
            --plan maf-package-plan.json \
            --results maf-diff-results.json \
            --dotnet-inspect dotnet-inspect \
            --timeout-seconds 180
          cat maf-diff-results.json

      - name: Fetch release notes
        id: release-notes
        continue-on-error: true
        # Phase 7.G fixup — env-redirect (see package-plan step above). NOTE: this step
        # previously had TWO `env:` keys (NEW here + GH_TOKEN below) — a YAML
        # duplicate-key the GitHub workflow parser REJECTS (startup_failure: the
        # 0-second push runs), and which PyYAML silently tolerated by keeping the
        # LAST block, so `NEW` was dropped and release-notes fetched empty tags.
        # Merged into one block. [final-review fix]
        env:
          NEW: ${{ needs.check-for-new-maf-release.outputs.new_version }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # MAF releases are on github.com/microsoft/agent-framework (NOT microsoft/agents).
          # MAF release tags follow the form `dotnet-X.Y.Z` (e.g. `dotnet-1.3.0`), per the
          # canonical release links in guides/maf-1.3.0-migration-guide.md.
          # Try both forms — `dotnet-X.Y.Z` first (canonical), `vX.Y.Z` as a fallback.
          for tag in "dotnet-${NEW}" "v${NEW}"; do
            if gh release view "$tag" --repo microsoft/agent-framework --json body --jq .body > release-notes.txt 2>/dev/null; then
              echo "✅ Fetched release notes from tag $tag"
              break
            fi
          done
          if [ ! -s release-notes.txt ]; then
            echo "Release notes unavailable for automated classification. MAF $NEW has no readable GitHub release body." > release-notes.txt
            echo "::warning::Release notes are unavailable for $NEW; classification will fail closed to breaking."
          fi
          # The upstream body is untrusted and may contain GitHub workflow-command
          # syntax (for example a line beginning `::error::`). Keep the raw body
          # only in the artifact/guide evidence; logs receive bounded metadata.
          NOTES_BYTES=$(wc -c < release-notes.txt)
          NOTES_SHA256=$(sha256sum release-notes.txt | cut -d' ' -f1)
          echo "Release-note evidence captured: tag=$NEW bytes=$NOTES_BYTES sha256=$NOTES_SHA256"

      - name: Update compatibility matrix
        env:
          OLD_VERSION: ${{ needs.check-for-new-maf-release.outputs.old_version }}
          NEW_VERSION: ${{ needs.check-for-new-maf-release.outputs.new_version }}
        run: python3 .github/scripts/update_compat_matrix.py

      - name: Sync code compatibility matrix (CompatibilityTool.cs)
        # Green-on-arrival: add the matching CompatibilityTool.Matrix entry so the
        # doc<->code drift test is green on the scaffold PR, instead of red until a
        # human hand-edits the tool. Runs AFTER the matrix-doc step — it reads the
        # row that step just wrote.
        env:
          OLD_VERSION: ${{ needs.check-for-new-maf-release.outputs.old_version }}
          NEW_VERSION: ${{ needs.check-for-new-maf-release.outputs.new_version }}
        run: python3 .github/scripts/sync_compat_tool.py

      - name: Generate draft guide section
        env:
          OLD_VERSION: ${{ needs.check-for-new-maf-release.outputs.old_version }}
          NEW_VERSION: ${{ needs.check-for-new-maf-release.outputs.new_version }}
        run: python3 .github/scripts/gen_guide_section.py

      - name: Restore maf-doctor (locked mode)
        # Round-2 review fixup — docs/security.md and threat-model.md both
        # claimed "CI restores in --locked-mode" as a blanket statement, but
        # this job (one of the 4 privileged, write-scoped jobs F-06/F-13
        # hardened against floating/unreviewed installs) ran a plain
        # `dotnet build` with an implicit, non-strict restore — a drifted
        # dependency graph would be silently re-resolved instead of failing
        # closed. Matches build-test.yml's restore-then-build split.
        continue-on-error: true   # same fault-tolerance boundary as the build step below
        run: dotnet restore src/maf-autopilot/maf-autopilot.csproj --locked-mode

      - name: Build maf-doctor from this checkout
        # F-06 (2026-07-19 security assessment) — this job has contents:write,
        # pull-requests:write, issues:write, actions:write and a maintainer PAT
        # that bypasses branch protection. It previously ran `dotnet tool
        # install --global maf-doctor --prerelease`, executing whatever the
        # newest floating prerelease on NuGet happened to be — not the reviewed
        # code at this commit — inside that privileged context. Building from
        # the already-checked-out, already-reviewed source removes the floating
        # dependency entirely: what runs below is exactly this commit.
        #
        # continue-on-error: true — verified-in-review fixup: the install line
        # this replaced lived INSIDE "Auto-append draft registry entries",
        # which is (and remains) continue-on-error: true with the explicit
        # intent "never block the PR on registry extraction." Hoisting the
        # build into its own step without the same setting turned a
        # contained, nice-to-have failure into a hard abort of the whole job
        # — including the version-bump PR the workflow actually exists to
        # open — on any transient build/restore hiccup. This restores the
        # original fault-tolerance boundary.
        continue-on-error: true
        run: dotnet build src/maf-autopilot/maf-autopilot.csproj -c Release -f net8.0 --no-restore

      - name: Auto-append draft registry entries
        continue-on-error: true   # never block the PR on registry extraction
        run: |
          MAF_DOCTOR="src/maf-autopilot/bin/Release/net8.0/maf-doctor.dll"
          if [ ! -f "$MAF_DOCTOR" ]; then
            echo "::warning title=Registry extraction skipped::maf-doctor build did not produce $MAF_DOCTOR (see the 'Build maf-doctor from this checkout' step above) — skipping draft registry extraction for this run."
            exit 0
          fi

          REGISTRY=".github/skills/maf-obsolete-api-registry/registry.yaml"
          TMP_ENTRIES="/tmp/draft-registry-entries.yaml"

          # Consume only independently validated artifacts from the package
          # plan. --diff-file eliminates the extractor's former second network
          # diff; --release-version keeps applies_to_codebases stable when a
          # package's exact preview version differs from the MAF train; id_scope
          # prevents non-Core registry ID collisions.
          extract_rc=0
          python3 .github/scripts/extract_registry_from_plan.py \
            --plan maf-package-plan.json \
            --results maf-diff-results.json \
            --maf-doctor "$MAF_DOCTOR" \
            --output "$TMP_ENTRIES" \
            --extraction-results maf-registry-extraction-results.json \
            --timeout-seconds 120 || extract_rc=$?

          # Phase 3.4 — de-dupe extracted entries against the existing registry
          # BEFORE appending. Re-runs (or the daily cron re-detecting the same
          # version before .maf-version catches up) must not accrete duplicate
          # draft entries. The helper prints only entry blocks whose `id:` is not
          # already present (and de-dupes within the batch).
          if ! NEW_ENTRIES=$(python3 .github/scripts/dedupe_registry_entries.py "$REGISTRY" "$TMP_ENTRIES"); then
            # A same-ID/different-identity collision means the extractor ledger
            # cannot truthfully claim those generated IDs were appended.
            rm -f maf-registry-extraction-results.json
            echo "::error::Registry de-duplication failed; invalidated extraction ledger."
            exit 1
          fi
          if [ -n "$NEW_ENTRIES" ]; then
            {
              echo ""
              echo "# === Draft entries auto-appended by release-watcher on $(date -u +%Y-%m-%d). Review before merging. ==="
              printf '%s\n' "$NEW_ENTRIES"
            } >> "$REGISTRY"
            echo "✅ Draft registry entries appended."
          else
            echo "ℹ️ No new draft registry candidates (all already present, or none extracted)."
          fi
          if [ "$extract_rc" -ne 0 ]; then
            echo "::warning::One or more planned registry extractions were unavailable or failed; the release verdict remains fail-closed."
            exit "$extract_rc"
          fi

      - name: Classify release (additive vs breaking)
        # Green-on-arrival verdict, decided in one place (release_classification):
        # additive ⇒ the scaffold is auto-filled and should be green; breaking ⇒
        # draft registry entries (or a review draft when a behavioral/lifecycle break
        # has no API draft) keep the PR red until reviewed and filled.
        id: classify
        run: |
          VERDICT=$(python3 .github/scripts/release_classification.py)
          echo "verdict=$VERDICT" >> "$GITHUB_OUTPUT"
          echo "::notice::MAF release classified as: $VERDICT"

      - name: Guard — a breaking release must leave a red gate
        # The 'breaking' verdict carries no gating force on its own; the PR only
        # goes red if registry-extract produced unfilled drafts. A behavioral
        # release-note-only behavioral break, lifecycle split, or failed package
        # diff may produce zero API drafts. Append a general REVIEW draft when no
        # current-release entry exists, plus one for each breaking lifecycle event
        # not explicitly covered by a current package entry, so unrelated Core
        # drafts cannot hide a package split.
        if: steps.classify.outputs.verdict == 'breaking'
        env:
          NEW_VERSION: ${{ needs.check-for-new-maf-release.outputs.new_version }}
          VERDICT: ${{ steps.classify.outputs.verdict }}
        run: python3 .github/scripts/ensure_breaking_gated.py

      - name: Upload diff artifacts for reviewer
        # Upload the plan, diff/extraction ledgers, and every dynamic per-surface artifact.
        # Failed/truncated raw output is deliberately retained for diagnosis;
        # consumers trust only result rows that pass strict validation.
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: maf-diff-${{ needs.check-for-new-maf-release.outputs.new_version }}
          path: |
            maf-package-plan.json
            maf-diff-results.json
            maf-registry-extraction-results.json
            diff-*.txt
            release-notes.txt
          retention-days: 30
          if-no-files-found: warn

      - name: Gate — require complete release evidence before commit
        # Classification deliberately fails closed to "breaking", but a verdict
        # alone is not permission to advance the release chain. Missing notes,
        # an unverifiable planned package, any invalid diff, or a missing/malformed
        # per-surface registry-extraction ledger must stop before .maf-version and
        # the watcher branch are written. Diagnostics were uploaded by the
        # preceding always() step.
        run: python3 .github/scripts/release_classification.py --require-complete-evidence

      - name: Update .maf-version
        # Phase 7.G fixup — env-redirect needs.*.outputs.* (same pattern as
        # the package-plan / release-notes steps above).
        env:
          NEW: ${{ needs.check-for-new-maf-release.outputs.new_version }}
        run: |
          echo "$NEW" > .maf-version
          echo "Updated .maf-version to $NEW"

      - name: Record immutable scaffold obligations
        # This data contract is generated only after every watcher-owned file is
        # final.  AI-fill verification recovers this exact blob from the first
        # scaffold commit and runs trusted-base verifier code against PR HEAD.
        env:
          OLD_VERSION: ${{ needs.check-for-new-maf-release.outputs.old_version }}
          NEW_VERSION: ${{ needs.check-for-new-maf-release.outputs.new_version }}
          BASE_COMMIT: ${{ github.sha }}
          PUSH_TARGET: ${{ inputs.push_target }}
        run: |
          BRANCH="${PUSH_TARGET:-release-watcher/maf-${NEW_VERSION}}"
          python3 .github/scripts/build_scaffold_obligations.py \
            --old-version "$OLD_VERSION" \
            --target-version "$NEW_VERSION" \
            --base-commit "$BASE_COMMIT" \
            --target-branch "$BRANCH" \
            --output ".github/maf-scaffold-obligations/maf-${NEW_VERSION}.json"

      - name: Commit to a release-watcher branch and open a PR
        id: commit_to_main
        # Phase 3.2 — minor/patch bumps used to auto-commit straight to main; MAJOR
        # bumps escalate to a human (see the escalation step below).
        if: needs.check-for-new-maf-release.outputs.is_major != 'true'
        # C (2026-06-28): the scaffold carries TODO placeholders that `verify-registry`
        # (a build-test gate) deliberately REJECTS, so pushing it straight to main turned
        # main RED and blocked every downstream PR (incident: MAF 1.11.0, run 28153665314).
        # We now push to a per-version branch and open a PR — main only ever receives the
        # FILLED, build-green result on merge. This also restores the B.H2 review gate the
        # earlier direct-to-main mode removed. `push_target` still allows a dry-run branch.
        env:
          NEW_VERSION: ${{ needs.check-for-new-maf-release.outputs.new_version }}
          OLD_VERSION: ${{ needs.check-for-new-maf-release.outputs.old_version }}
          IS_MAJOR: ${{ needs.check-for-new-maf-release.outputs.is_major }}
          VERDICT: ${{ steps.classify.outputs.verdict }}
          # A human token is intentional here: GitHub suppresses pull_request
          # workflow events for branches/PRs created with GITHUB_TOKEN. Never
          # fall back silently, or the scaffold PR opens without its CI checks.
          GH_TOKEN: ${{ secrets.COPILOT_ASSIGN_PAT }}
          PUSH_TARGET: ${{ inputs.push_target }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}  # REP-34
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          # Stage only the paths the watcher is supposed to touch (artefacts like
          # diff-*.txt / release-notes.txt are uploaded separately, not committed).
          git add .maf-version \
                  .github/maf-scaffold-obligations/maf-${NEW_VERSION}.json \
                  docs/compatibility-matrix.md \
                  src/maf-autopilot/Tools/CompatibilityTool.cs \
                  guides/*.md \
                  .github/skills/maf-obsolete-api-registry/registry.yaml

          # Bail cleanly if every preceding step was a no-op.
          if git diff --cached --quiet; then
            echo "ℹ️ No watcher changes to commit — pipeline was a no-op."
            echo "committed=false" >> $GITHUB_OUTPUT
            exit 0
          fi

          # Commit message via a Python helper (avoids the backtick-in-heredoc footgun
          # that aborted the watcher with exit 127 on 2026-05-17, run 25994841252).
          python3 .github/scripts/build_watcher_commit_message.py \
            > "$RUNNER_TEMP/commit-msg.txt"

          # Push to a per-version branch (NOT main) so an unfilled scaffold can never red
          # main. A manual dispatch may override the branch via push_target (dry-run).
          BRANCH="${PUSH_TARGET:-release-watcher/maf-${NEW_VERSION}}"
          # Hard guard: never operate on the default/protected branch. The whole point of C
          # is that the scaffold goes through a PR — a stray push_target=main must NOT push.
          if [ "$BRANCH" = "main" ] || [ "$BRANCH" = "master" ]; then
            echo "::error::push_target must not be a protected branch (got '$BRANCH'). The watcher opens a PR; it never pushes the scaffold to main."
            exit 1
          fi
          # WM-12 (defense-in-depth for the check-for-new-maf-release short-circuit):
          # if the scaffold branch already exists on the remote, a PR is in flight —
          # leave it and its in-flight fill commits untouched instead of force-clobbering
          # them (which the old --force-with-lease then failed on in a fresh clone, red-ing
          # the whole job). Public repo → unauthenticated ls-remote works.
          if git ls-remote --exit-code --heads origin "$BRANCH" >/dev/null 2>&1; then
            echo "ℹ️ Branch $BRANCH already exists on the remote — scaffold PR in flight; leaving it (and any fill commits) untouched. Delete the branch or dispatch with push_target to regenerate."
            echo "committed=false" >> $GITHUB_OUTPUT
            exit 0
          fi
          git checkout -b "$BRANCH"
          git commit -F "$RUNNER_TEMP/commit-msg.txt"
          # SEC-27: pass the ephemeral auth header through Git's one-command
          # environment config, not `.git/config`, the remote URL, or argv.
          # --force-with-lease (not -f) lets a deliberate regenerate update its
          # own branch without clobbering someone else's work.
          AUTH_VALUE="AUTHORIZATION: basic $(printf 'x-access-token:%s' "$GH_TOKEN" | base64 -w0)"
          GIT_CONFIG_COUNT=1 \
          GIT_CONFIG_KEY_0=http.extraheader \
          GIT_CONFIG_VALUE_0="$AUTH_VALUE" \
            git push --force-with-lease origin "HEAD:${BRANCH}"
          unset AUTH_VALUE
          echo "branch=$BRANCH" >> $GITHUB_OUTPUT
          echo "committed=true" >> $GITHUB_OUTPUT

          # Open a PR to main (idempotent).
          if gh pr view "$BRANCH" --json number >/dev/null 2>&1; then
            echo "✅ Updated existing PR for branch $BRANCH."
          else
            # REP-34: build the body with printf + SINGLE-quoted lines (no command
            # substitution, so backticks + markdown bullets are safe — this is what
            # closed the 2026-05-17 backtick incident) and pass --body-file. printf
            # keeps every line a normal, properly-indented YAML block line (no heredoc
            # de-indent trap), restoring markdown formatting + the run-evidence link.
            if [ "$VERDICT" = "additive" ]; then
              printf '%s\n' \
                'Auto-generated by the release watcher for MAF `__OLD__` -> `__NEW__`. Classified **ADDITIVE** (no .NET `[BREAKING]` notes and no removed members): the compatibility matrix, the code matrix (`CompatibilityTool.cs`), and the migration guide were filled deterministically, so this PR should be **green on arrival**.' \
                '' \
                '**Review checklist**' \
                '- [ ] Carried-forward package pins are correct' \
                '- [ ] The new-member list matches the upstream diff' \
                '- [ ] CI is green' \
                '' \
                'Raw API diffs + release notes (Artifacts section): __RUN_URL__' \
                > "$RUNNER_TEMP/pr-body.md"
            else
              printf '%s\n' \
                'Auto-generated by the release watcher for MAF `__OLD__` -> `__NEW__`. Classified **BREAKING**: the registry has draft entries whose migration text a human must write. The keep-breaking-red gate keeps this PR **red** until the current-version drafts are filled — by design, so an unfilled scaffold can never reach main.' \
                '' \
                '**To complete**' \
                '- [ ] Fill the registry/guide TODOs (assign GitHub Copilot to the PR, or edit directly)' \
                '- [ ] Confirm CI is green, then merge' \
                '' \
                'Raw API diffs + release notes (Artifacts section): __RUN_URL__' \
                > "$RUNNER_TEMP/pr-body.md"
            fi
            # The single-quoted printf lines kept these literal (no shell expansion risk);
            # substitute the trusted, semver-validated values in now.
            sed -i "s|__OLD__|${OLD_VERSION}|g; s|__NEW__|${NEW_VERSION}|g; s|__RUN_URL__|${RUN_URL}|g" "$RUNNER_TEMP/pr-body.md"
            gh pr create --base main --head "$BRANCH" \
              --title "chore: MAF ${NEW_VERSION} registry/matrix/guide update (${VERDICT})" \
              --body-file "$RUNNER_TEMP/pr-body.md"
            echo "✅ Opened PR for branch $BRANCH (${VERDICT})."
            # Label additive PRs so the auto-merge graduation (after a few proven
            # releases) can target them. Label ops are best-effort — never block.
            if [ "$VERDICT" = "additive" ]; then
              gh label create additive-auto-green --color 0E8A16 \
                --description "Watcher PR: additive release, scaffold auto-filled & green" 2>/dev/null || true
              gh pr edit "$BRANCH" --add-label additive-auto-green 2>/dev/null || true
            fi
          fi

      - name: Summarize next steps (fill the PR, then merge)
        if: ${{ steps.commit_to_main.outputs.committed == 'true' }}
        env:
          BRANCH: ${{ steps.commit_to_main.outputs.branch }}
          TARGET: ${{ needs.check-for-new-maf-release.outputs.new_version }}
        run: |
          # C (2026-06-28): we no longer auto-dispatch a SEPARATE Copilot fill PR off main.
          # That PR's workflows are approval-gated and historically stalled (incident: the
          # MAF 1.11.0 copilot/fill-todos PR sat unapproved for days). The scaffold now lives
          # on THIS run's PR branch — fill it there (assign GitHub Copilot to the PR, or edit
          # directly), confirm build-test is green, then merge. maf-ai-fill-todos.yml remains
          # available for manual use if you retarget it to the branch.
          echo "::notice::MAF $TARGET scaffold PR opened on branch $BRANCH — fill the registry/guide TODOs, confirm build-test is green, then merge."

      - name: Escalate MAJOR version bump for manual review
        # Phase 3.2 — major bumps (x.y → (x+1).0) carry the highest breaking-
        # change risk, so they are NOT auto-committed to main. Detect + diff
        # still ran (artifacts are attached to this run); here we open a single
        # tracking issue and let a human drive the migration. is_major flows via
        # env: redirect (ci-invariants job 4).
        if: needs.check-for-new-maf-release.outputs.is_major == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NEW: ${{ needs.check-for-new-maf-release.outputs.new_version }}
          OLD: ${{ needs.check-for-new-maf-release.outputs.old_version }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          gh label create maf-release  --color 0E8A16 --force || true
          gh label create needs-review --color FBCA04 --force || true
          TITLE="🚨 MAJOR MAF bump ${OLD} → ${NEW} — manual review required"
          NUM=$(gh issue list --label maf-release --state open --json number,title \
            --jq ".[] | select(.title == \"${TITLE}\") | .number" | head -1)
          if [ -n "$NUM" ]; then
            echo "Major bump already tracked in issue #${NUM} — leaving as-is."
          else
            gh issue create --title "${TITLE}" --label "maf-release,needs-review" \
              --body "A new **major** MAF release was detected: \`${OLD}\` → \`${NEW}\`. Major bumps are not auto-committed to main (breaking-change risk). The breaking-API diffs are attached as artifacts on the run: ${RUN_URL}. To process: review the diffs, then dispatch the watcher manually for this version or apply the changes by hand."
            echo "Opened escalation issue for major bump ${OLD} → ${NEW}."
          fi

  # Phase 2.1 — surface ANY watcher failure as a GitHub notification. Scheduled
  # runs failed silently for ~5 weeks because the only signal was a red X in the
  # Actions tab. This opens (or de-dupes onto) a maf-release-labelled tracking
  # issue so the maintainer actually hears about it. github.* contexts flow via
  # env: redirect to satisfy ci-invariants job (4).
  notify-on-failure:
    needs: [validate-inputs, check-for-new-maf-release, analyze-and-update]
    if: failure()
    runs-on: ubuntu-latest
    permissions:
      issues: write
    steps:
      - name: Open or update a failure tracking issue
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          WORKFLOW: ${{ github.workflow }}
        run: |
          gh label create maf-release --color 0E8A16 \
            --description "Auto-generated by maf-release-watcher / fill workflows" --force || true
          TITLE="🔴 Self-update workflow failed: ${WORKFLOW}"
          # Exact-title match (not --search, which is flaky on emoji) so repeat
          # failures update one rolling issue instead of spamming new ones.
          NUM=$(gh issue list --label maf-release --state open --json number,title \
            --jq ".[] | select(.title == \"${TITLE}\") | .number" | head -1)
          if [ -n "$NUM" ]; then
            gh issue comment "$NUM" --body "Failed again: ${RUN_URL}"
            echo "Commented on existing failure issue #${NUM}"
          else
            gh issue create --title "${TITLE}" --label maf-release \
              --body "The scheduled **${WORKFLOW}** workflow failed. Most recent failed run: ${RUN_URL}. Updated (not duplicated) on subsequent failures; close once fixed."
            echo "Opened new failure issue"
          fi


